[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'
extend-exclude = '''
(
  # The following are specific to Black, you probably don't want those.
  | images/data
  | pdfs/data
  | venv
)
'''
